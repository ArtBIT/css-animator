import React from "react";
import { DEFAULT_ANIMATION_CONTEXT } from "../constants";

export const AnimationContext = React.createContext(DEFAULT_ANIMATION_CONTEXT);
Unknown template.
export default AnimationContext;
